#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "afs_york_ward_profiles")"
[ "$CONDA_DEFAULT_ENV" = "afs_york_ward_profiles" ] || echo "Have you run \`make install\`?"
